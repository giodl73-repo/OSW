# Required for NOAA NCSS, RG Argo, and range-readable RTOFS pipelines.
netCDF4==1.7.4
# Optional range-readable HDF5/NetCDF path used by NOAA RTOFS refresh scripts.
aiohttp==3.14.3
fsspec==2026.7.0
h5netcdf==1.8.1
h5py==3.16.0
# Decodes NOAA GFS GRIB2 surface-flux messages. Native wheels may lag the
# newest Python release; Python 3.12 is the tested acquisition environment.
eccodes==2.48.0
